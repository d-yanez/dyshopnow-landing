options:
  logging: CLOUD_LOGGING_ONLY

steps:
  # 🔨 Build Docker image
  - id: 'Build image'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - --platform=linux/amd64
      - -t
      - "gcr.io/$PROJECT_ID/dyshopnow-landing:$SHORT_SHA"
      - .

  # 📤 Push Docker image
  - id: 'Push image'
    name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - "gcr.io/$PROJECT_ID/dyshopnow-landing:$SHORT_SHA"

  # 🚀 Deploy to Cloud Run
  - id: 'Deploy to Cloud Run'
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - -c
      - |
        gcloud run deploy dyshopnow-landing \
          --image gcr.io/$PROJECT_ID/dyshopnow-landing:$SHORT_SHA \
          --region=us-central1 \
          --platform=managed \
          --service-account=sa-dyshopnow-landing@${PROJECT_ID}.iam.gserviceaccount.com \
          --allow-unauthenticated \
          --port=8080 \
          --timeout=300s \
          --memory=256Mi \
          --cpu=1 \
          --concurrency=10 \
          --min-instances=0 \
          --max-instances=1

images:
  - "gcr.io/$PROJECT_ID/dyshopnow-landing:$SHORT_SHA"
